version: '3.8'

services:
  om1_avatar:
    build:
      context: .
      dockerfile: Dockerfile
    image: openmindagi/om1_avatar:latest
    container_name: om1_avatar
    environment:
      - VITE_OM_API_KEY=${OM_API_KEY}
      - VITE_OM_API_KEY_ID=${OM_API_KEY_ID}
      - VITE_SHOW_SYSTEM_STATUS=${SHOW_SYSTEM_STATUS:-false}
      - VITE_SHOW_ACTIVITY_STATE=${SHOW_ACTIVITY_STATE:-false}
    restart: unless-stopped
    network_mode: host
    ports:
      - "4173:4173"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
